# Security policy

The Once policy flake is a trust root. Report suspected vulnerabilities
privately to the Closure Labs maintainers. Do not include signing keys or
production cache credentials in a public issue.

Compromise of this repository, an accepted signing key, or the protected
workflow that pins the policy revision can weaken Once decisions. Affected keys
and policy revisions must be revoked together.
